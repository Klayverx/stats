import {extendTheme} from '@chakra-ui/react';

export const statsTheme = extendTheme({
	fonts: {
		body: 'Inter, sans-serif',
	},
	fontWeight: {
		normal: 400,
		medium: 600,
		bold: 700,
	},
	colors: {
		primary: '#FC4C02',
	},
});
