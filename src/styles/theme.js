import {extendTheme} from '@chakra-ui/react';

export const statsTheme = extendTheme({
	fonts: {
		body: 'Roboto, -apple-system',
	},
	fontWeight: {
		normal: 400,
		medium: 600,
		bold: 700,
	},
	colors: {
		primary: '#FC4C02',
	},
});
